## aliases
# ls color aliases
alias ls='ls -FG'
alias ll='ls -alFG'
# rm/cp/mv
alias rm   = 'rm -i'
alias cp   = 'cp -i'
alias mv   = 'mv -i'
alias tree = 'tree -C'

# terraform command aliases
alias tf='terraform'

# docker buildkit active
export DOCKER_BUILDKIT=1

