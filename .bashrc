## aliases
# ls color aliases
alias ls='ls -FG'
alias ll='ls -alFG'
# rm/cp/mv
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'
alias tree='tree -C'

# terraform command aliases
alias tf='terraform'

# docker buildkit active
export DOCKER_BUILDKIT=1

export PATH=$PATH:/Users/no136/.nodebrew/current/bin
export PATH=$PATH:~/.nodebrew/current/bin
